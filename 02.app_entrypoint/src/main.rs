#![allow(dead_code)] // ex: unused variables
#![allow(unused_imports)]

fn main() {
    println!("test");
}

// main function is invoked when the program starts where and as a result you cannot have several main functions
// println! is a macro which to outputs to the console
// the first 2 instructions turn off some of the warning that the rust compiler gives you
